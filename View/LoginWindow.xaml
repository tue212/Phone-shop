<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PhoneShop.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhoneShop"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LoginWindow"
    Activated="Window_Activated">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Image Source="/Assets/BGShop.png" Stretch="UniformToFill"/>
            <TextBlock Text=" Tue's shop" Foreground="white" Width="300" Height="75" FontSize="50" FontWeight="Bold" />

        </Grid>
        <Grid Grid.Column="1">
            <StackPanel Width="400" Height="500" >
                <TextBlock Text="Login" Style="{StaticResource TitleLargeTextBlockStyle}" Margin="15" />
                <TextBox Name="usernameTextBox" Header="Username:" Margin="15" />
                <PasswordBox Name="passwordBox" Header="Password" PasswordChar="*" Margin="15" />
                <CheckBox Name="rememberMeCheckBox" Content="Remember me" Margin="15" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Login" Click="loginButton_Click" Style="{StaticResource ButtonRevealStyle}" Margin="15" />
                    <Button Content="Update" />
                </StackPanel>

                <StackPanel HorizontalAlignment="Center" >
                    <HyperlinkButton Content="Config" Width="80" Height="35" Margin="0,50,0,0" />
                </StackPanel>
            </StackPanel>
        </Grid>
        
    </Grid>
</Window>
