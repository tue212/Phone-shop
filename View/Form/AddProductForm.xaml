<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="PhoneShop.View.Form.AddProductForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhoneShop.View.Form"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBox Header="Tên Sản phẩm" Text="{x:Bind NewItem.Name, Mode=TwoWay}"/>
        <TextBox Header="Giá bán" Text="{x:Bind NewItem.SalePrice, Mode=TwoWay}"/>
        <TextBox Header="Loại sản phẩm" Text="{x:Bind NewItem.CatId, Mode=TwoWay}"/>
        <TextBox Header="Mô tả" Text="{x:Bind NewItem.Description, Mode=TwoWay}"/>
        <TextBox Header="Giá nhập" Text="{x:Bind NewItem.ImportPrice, Mode=TwoWay}"/>
        <TextBox Header="Số lượng" Text="{x:Bind NewItem.Quantity, Mode=TwoWay}"/>
        <TextBox Header="Url Hình ảnh" Text="{x:Bind NewItem.ImagePath, Mode=TwoWay}"/>
    </StackPanel>
</ContentDialog>
