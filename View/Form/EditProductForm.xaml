<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="PhoneShop.View.Form.EditProductForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhoneShop.View.Form"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBox Header="Tên Sản phẩm" Text="{x:Bind EditItem.Name, Mode=TwoWay}"/>
        <TextBox Header="Giá bán" Text="{x:Bind EditItem.SalePrice, Mode=TwoWay}"/>
        <TextBox Header="Loại sản phẩm" Text="{x:Bind EditItem.CatId, Mode=TwoWay}"/>
        <TextBox Header="Mô tả" Text="{x:Bind EditItem.Description, Mode=TwoWay}"/>
        <TextBox Header="Giá nhập" Text="{x:Bind EditItem.ImportPrice, Mode=TwoWay}"/>
        <TextBox Header="Số lượng" Text="{x:Bind EditItem.Quantity, Mode=TwoWay}"/>
        <TextBox Header="Url Hình ảnh" Text="{x:Bind EditItem.ImagePath, Mode=TwoWay}"/>
    </StackPanel>
</ContentDialog>
